# OpenAI API Key
OPENAI_API_KEY="INPUT_YOUR_API_KEY_HERE"

# Model settings (JSON string — must be parsed in code)
MODEL_NAMES_JSON='{
  "general_purpose": "gpt-4o",
  "high_capacity": "gpt-4-turbo",
  "fast": "gpt-3.5-turbo-0125"
}'

# Embedding model options
EMBEDDING_MODELS_JSON='{
  "small": "text-embedding-3-small",
  "large": "text-embedding-3-large"
}'

# Optional: Defaults (override in code if needed)
DEFAULT_MODEL_NAME="gpt-4o"
DEFAULT_EMBEDDING_MODEL="text-embedding-3-small"


# ChromaDB settings
MAX_TOKENS=15000

# Chunking settings
CHUNK_SIZE=1000
CHUNK_OVERLAP=100

# Retrieval settings
RETRIEVAL_K=5