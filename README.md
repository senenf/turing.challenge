# turing.challenge
Solution to the requested data science challenge: RAG-based chatbot using LangChain, PDF ingestion, dynamic memory, Python code execution, theoretical Q&amp;A on GenAI concepts, and an optional Dockerized object detection API using pre-trained models.
